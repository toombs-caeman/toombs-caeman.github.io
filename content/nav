`nav_pages="index.txt bigf/index.txt"`
<header>
<div .nav-menu>
    <input #burger type='checkbox' style='display:none'>
    <label .burger for='burger'-> `for i in {1..3}; do echo "<div .bbar$i ><div .bar$i></div></div>"; done`
    <nav aria-label="Main Navigation"-> <ul-> `for p in $nav_pages; do echo "<li>";page_link $p; done`
</div>
<h1 .title -> `? title`
</header>

